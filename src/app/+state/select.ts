import { ExpState } from './reducer';

export const {
  selectUser,
  selectFollows,
  selectPost,
  selectUserLoginedSavedPosts,
  selectZoom,
  selectSearchPool,
  selectGroupCats,
  selectPostOfUser,
  selectUserProfile,
  selectCategory,
  selectMessage,
  selectAllMessage,
  selectProvinces,
  selectCitySelectedOfProvince,
  selectUserBoard,
  selectFilters
} = ExpState;
